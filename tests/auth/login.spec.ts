import { test, expect } from '@playwright/test';

// Auth flow tests — to be implemented on Day 3
// Covers: login, signup, logout, invalid credentials
// Page objects: LoginPage, SignupPage (to be built on Day 2)

// test.todo('successful login with valid credentials');
// test.todo('login fails with incorrect password');
// test.todo('login fails with unregistered email');
// test.todo('successful user registration');
// test.todo('registration fails with existing email');
// test.todo('logout clears session and redirects to home');
